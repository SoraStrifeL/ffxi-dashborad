# Dependencies
node_modules/

# Environment — never commit secrets
.env

# Runtime state
calibrations.json

# Runtime config written by the dashboard — contains tokens/credentials
data/*.json
!data/scripts.json

# Build artifacts (Docker builds these from src/ and client/)
dist/
public/assets/
*.tsbuildinfo

# Map images (648 MB — download separately; see README)
public/maps/

# User-uploaded custom images (server.js creates these dirs on startup)
public/uploads/

# Bulk map extraction working directory
map_download/

# Logs
*.log

# Editor / OS
.DS_Store
Thumbs.db

# Claude Code internal settings (project-specific)
.claude/

# FFXI client DAT files (large, copyrighted game data) — dropped in for the
# optional DAT fetcher; never commit them. The README is kept.
ffxi-dat/**
!ffxi-dat/
!ffxi-dat/README.md
!ffxi-dat/NEEDED.txt
!ffxi-dat/copy-from-client.sh

# Full retail FFXI client install (copyrighted game data) — used by
# tools/build-calibrations.js; never commit it.
FINAL FANTASY XI/

# subagent-driven-development scratch (progress ledger, task briefs, reports)
.superpowers/
